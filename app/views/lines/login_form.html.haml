#login
  - if @current_passenger
    %p.welcome  Welcome, #{@current_passenger.name}!
    = link_to 'Log Out', logout_path, :method => :post
  - else
    %p.login= link_to 'Log in with your Facebook account', login_path